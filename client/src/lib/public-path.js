'use strict';

// For Vite, the public path is configured in vite.config.js
// The import.meta.env.BASE_URL is automatically available
// and resolves to the same value as getUrl('client/') in production
